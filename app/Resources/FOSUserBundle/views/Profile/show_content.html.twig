{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    
    <p>{{ 'profile.show.username'|trans }}: <b>{{ user.username }}</b></p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
</div>
